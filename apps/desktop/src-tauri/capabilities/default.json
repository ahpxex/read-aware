{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set shared by every platform's main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
