{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities: window chrome, signed app updates, process relaunch, and the dev-time MCP bridge.",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "core:window:allow-start-dragging",
    "updater:default",
    "process:allow-restart",
    "mcp-bridge:default"
  ]
}
