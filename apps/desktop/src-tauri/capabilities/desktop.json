{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities: custom window chrome dragging and the dev-time MCP bridge (neither plugin is compiled for mobile).",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": ["core:window:allow-start-dragging", "mcp-bridge:default"]
}
