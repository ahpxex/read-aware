# Dev sessions call the local relay (`bun run dev` in apps/relay) so the
# pricing page's checkout exercises the sandbox Stripe keys in .dev.vars.
# Vite only loads this file in dev mode; production builds fall through to
# https://relay.readaware.app (src/lib/site.ts).
VITE_RELAY_URL=http://localhost:8787
