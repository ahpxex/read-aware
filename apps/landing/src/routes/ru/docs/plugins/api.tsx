import { Link, createFileRoute } from "@tanstack/react-router";
import { MARKETPLACE_REPO_URL } from "../../../../lib/site";

export const Route = createFileRoute("/ru/docs/plugins/api")({
  head: () => ({
    meta: [
      { title: "Справочник API плагинов — Документация ReadAware" },
      {
        name: "description",
        content:
          "Текущий контракт плагинов ReadAware: манифест, возможности, домены, расширения, службы, декларативный интерфейс, жизненный цикл и миграции.",
      },
    ],
  }),
  component: PluginApiPage,
});

function PluginApiPage() {
  return (
    <article className="doc-prose">
      <h1>Справочник API плагинов</h1>
      <p className="lead">
        Плагин — это папка с <code>manifest.json</code> и собранным модулем ES.
        Точный публичный контракт TypeScript поставляется в виде{" "}
        <code>types/plugin-api.d.ts</code> в{" "}
        <a href={MARKETPLACE_REPO_URL} target="_blank" rel="noopener noreferrer">
          репозитории readaware-plugins
        </a>. На этой странице объясняется, как связаны его части.
      </p>

      <h2>Структура пакета</h2>
      <pre><code>{`my-plugin/
  manifest.json
  main.js
  src/main.ts       # recommended and committed for review
  assets/           # optional, explicitly listed for marketplace installs`}</code></pre>
      <p>
        <code>main.js</code> экспортирует по умолчанию объект жизненного цикла. ReadAware
        запускает его в отдельном модульном Worker и передаёт <code>activate</code>
        контекст, ограниченный актором.
      </p>
      <pre><code>{`export default {
  activate(ctx) {
    // Inspect and register. Side effects are blocked in this phase.
  },
  migrate(storageCtx, change) {
    // Optional: transform plugin-private KV and documents.
  },
  deactivate() {
    // Optional: release the plugin's own external resources.
  },
};`}</code></pre>

      <h2>Манифест</h2>
      <pre><code>{`{
  "id": "theme-schedule",
  "name": "Расписание тем",
  "version": "0.1.0",
  "schemaVersion": 1,
  "minAppVersion": "0.3.0",
  "requires": {
    "domains": { "settings": "^1.0.0" },
    "contributions": {
      "commands": "^1.0.0",
      "settingsOptions": "^1.0.0"
    },
    "services": {
      "storage": "^1.0.0",
      "schedules": "^1.0.0",
      "ui": "^1.0.0"
    },
    "schemas": { "settings": "^1.0.0" }
  },
  "settingsAccess": {
    "discover": ["appearance.theme", "reading.theme"],
    "write": ["appearance.theme", "reading.theme"]
  },
  "main": "main.js"
}`}</code></pre>
      <div className="overflow-x-auto">
        <table>
          <thead><tr><th>Поле</th><th>Контракт</th></tr></thead>
          <tbody>
            <tr><td><code>id</code></td><td>Строчные буквы, цифры и дефисы; максимум 64 символа. Это постоянное пространство имён, совпадающее с именем папки.</td></tr>
            <tr><td><code>name</code>, <code>version</code></td><td>Имя, видимое пользователю, и версия пакета.</td></tr>
            <tr><td><code>schemaVersion</code></td><td>Обязательное положительное целое число для приватных KV- и документных данных плагина. Не зависит от версии пакета.</td></tr>
            <tr><td><code>requires</code></td><td>Обязательная карта ID возможностей и диапазонов semver, сгруппированных по доменам, расширениям, службам и схемам.</td></tr>
            <tr><td><code>permissions</code></td><td>Необязательные семантические полномочия, запрошенные у пользователя. Неизвестные значения не проходят проверку.</td></tr>
            <tr><td><code>settingsAccess</code></td><td>Необязательные предоставления discover/read/write для точных путей настроек или явных групп <code>section.*</code>.</td></tr>
            <tr><td><code>minAppVersion</code></td><td>Необязательная минимальная версия приложения. Используйте её, когда пакет зависит от недавно выпущенной возможности.</td></tr>
            <tr><td><code>settings</code></td><td>Необязательные поля настроек плагина, отображаемые хостом.</td></tr>
            <tr><td><code>schedules</code></td><td>Необязательные периодические задачи, объявленные до привязки обработчиков.</td></tr>
            <tr><td><code>themes</code>, <code>fonts</code></td><td>Необязательные декларативные расширения тем и шрифтов; требуется <code>ui:themes</code>.</td></tr>
            <tr><td><code>main</code></td><td>Модуль входа относительно папки; по умолчанию <code>main.js</code>.</td></tr>
          </tbody>
        </table>
      </div>
      <p>
        Используйте <Link to="/ru/docs/plugins/capabilities">каталог возможностей</Link>{" "}
        для просмотра полного списка и словаря разрешений. Требование всегда
        является заявлением о совместимости и никогда не предоставляет полномочий.
      </p>

      <h2>Контекст среды выполнения</h2>
      <div className="overflow-x-auto">
        <table>
          <thead><tr><th>Пространство имён</th><th>Содержимое</th></tr></thead>
          <tbody>
            <tr><td><code>ctx.manifest</code></td><td>Проверенный манифест, только для чтения.</td></tr>
            <tr><td><code>ctx.appVersion</code>, <code>ctx.locale</code></td><td>Версия хоста и текущая локаль интерфейса.</td></tr>
            <tr><td><code>ctx.lifecycle.phase</code></td><td><code>activating</code>, <code>migrating</code> или <code>active</code>.</td></tr>
            <tr><td><code>ctx.capabilities</code></td><td>Только версии возможностей, видимые этому актору плагина.</td></tr>
            <tr><td><code>ctx.domains</code></td><td>Предоставленные состояние и поведение ReadAware.</td></tr>
            <tr><td><code>ctx.contributions</code></td><td>Реестры, в которые плагин может предоставлять реализации.</td></tr>
            <tr><td><code>ctx.services</code></td><td>Ограниченные операции хоста и приватная инфраструктура плагина.</td></tr>
          </tbody>
        </table>
      </div>
      <p>
        Пространства имён, защищённые разрешениями, отсутствуют, если их не предоставили. Каждый вызов
        Worker также авторизуется на стороне хоста; скрытие метода — не единственная проверка.
        Регистрации возвращают объект освобождения и удаляются в обратном порядке, если активация
        завершается ошибкой или плагин отключён.
      </p>

      <h2>Домены</h2>
      <p>
        Домен предоставляет <code>queries</code>, необязательные <code>commands</code>
        и <code>events.subscribe</code> для зафиксированных событий. Команды используют
        тот же путь записи через журнал событий, что и ReadAware, и относятся к{" "}
        <code>plugin:&lt;id&gt;</code>. Разрешение записи включает чтение.
      </p>
      <div className="overflow-x-auto">
        <table>
          <thead><tr><th>Домен</th><th>Запросы и команды</th><th>Полномочия</th></tr></thead>
          <tbody>
            <tr>
              <td><code>library</code></td>
              <td>Книги, метаданные, исходный текст глав, оглавление, коллекции; команды импорта, изменения, добавления в избранное, удаления, виртуальных книг и коллекций.</td>
              <td><code>library:read</code> / <code>library:write</code></td>
            </tr>
            <tr>
              <td><code>reading</code></td>
              <td>Статистика чтения по книге и в целом; отметить книгу завершённой, открыть книгу и перейти к CFI или href.</td>
              <td><code>reading:read</code> / <code>reading:write</code></td>
            </tr>
            <tr>
              <td><code>annotations</code></td>
              <td>Фильтрация выделений, заметок и пассивных следов вопросов; создание, изменение, перекрашивание и удаление выделений или заметок.</td>
              <td><code>annotations:read</code> / <code>annotations:write</code></td>
            </tr>
            <tr>
              <td><code>conversations</code></td>
              <td>Чтение книжных веток, список глобальных веток и чтение ветки. Записи остаются в среде выполнения чата.</td>
              <td><code>conversations:read</code></td>
            </tr>
            <tr>
              <td><code>settings</code></td>
              <td>Обнаружение разрешённых записей каталога, чтение разрешённых значений, обновление поддерживаемых целей и подписка на зафиксированные изменения.</td>
              <td>Точные предоставления <code>settingsAccess</code></td>
            </tr>
          </tbody>
        </table>
      </div>
      <p>
        Домена <code>shelf</code> или <code>appearance</code> не существует.
        Данные библиотеки и активное поведение чтения разделены. Внешний вид — это
        раздел внутри настроек.
      </p>

      <h3>Доступ к настройкам</h3>
      <p>
        <code>discover</code>, <code>read</code> и <code>write</code> независимы.
        По возможности предоставляйте точные пути; используйте группу раздела,
        например <code>appearance.*</code>, только если функции действительно нужен
        весь раздел. Обновления проходят проверку каталога, политику цели, сохранение
        и эффекты после фиксации.
      </p>
      <pre><code>{`const entries = await ctx.domains.settings.queries.discover({
  section: "appearance",
});

await ctx.domains.settings.commands.update([
  {
    path: "appearance.theme",
    value: "dark",
    target: { kind: "global" },
  },
]);`}</code></pre>

      <h2>Расширения</h2>
      <div className="overflow-x-auto">
        <table>
          <thead><tr><th>Реестр</th><th>Плагин предоставляет</th><th>Разрешение</th></tr></thead>
          <tbody>
            <tr><td><code>selectionActions</code></td><td>Действие выделения и обработчик, возвращающий уведомление или представление хоста.</td><td>Нет</td></tr>
            <tr><td><code>headerActions</code></td><td>Действие ридера или библиотеки, метаданные размещения и callback представления.</td><td>Нет</td></tr>
            <tr><td><code>commands</code></td><td>Метаданные команды и обработчик.</td><td>Нет</td></tr>
            <tr><td><code>settingsOptions</code></td><td>Динамические варианты для одного объявленного поля плагина.</td><td>Нет</td></tr>
            <tr><td><code>voiceProviders</code></td><td>Список голосов и синтез закодированного аудио.</td><td>Нет</td></tr>
            <tr><td><code>contentProviders</code></td><td>Разделы для ключа виртуальной книги.</td><td>Нет</td></tr>
            <tr><td><code>readerModes</code></td><td>Ограниченный режим сегментации ридера; пока только для встроенных плагинов.</td><td><code>reader:modes</code></td></tr>
            <tr><td><code>agentTools</code></td><td>Схема инструмента, понятная подпись, описание и исполнитель.</td><td><code>agent:tools</code></td></tr>
            <tr><td><code>agentContextProviders</code></td><td>Ограниченные справочные блоки текущего хода.</td><td><code>agent:context</code></td></tr>
            <tr><td><code>agentRetrievalProviders</code></td><td>Результаты поиска из данных плагина.</td><td><code>agent:retrieval</code></td></tr>
            <tr><td><code>memoryCandidateProviders</code></td><td>Возможные долговременные факты, предпочтения, идеи или сводки.</td><td><code>agent:memory</code></td></tr>
            <tr><td><code>themes</code>, <code>fonts</code></td><td>Смысловые данные тем и шрифтов, объявленные в манифесте.</td><td><code>ui:themes</code></td></tr>
          </tbody>
        </table>
      </div>
      <p>
        Каждый ID расширения ограничен пространством имён плагина, каждая регистрация
        принадлежит плагину и доступна для проверки, а устаревшие объекты освобождения
        не могут удалить более новую замену. Новый тип расширения всё ещё требует
        специально созданного потребителя хоста; после этого любой совместимый плагин
        может зарегистрироваться без явного упоминания в приложении.
      </p>

      <h3>Границы расширения агента</h3>
      <ul>
        <li><strong>Провайдеры контекста</strong> работают в течение одного хода. Хост добавляет происхождение, ограничивает размер и сериализует вывод как ненадёжные справочные данные.</li>
        <li><strong>Провайдеры поиска</strong> становятся именованными инструментами со схемой <code>query</code>/<code>limit</code>, которой владеет хост, и усечёнными результатами.</li>
        <li><strong>Провайдеры кандидатов памяти</strong> предлагают ограниченные кандидаты после хода; хост проверяет область, удаляет дубликаты и выполняет долговременную запись.</li>
      </ul>
      <p>
        Плагины никогда не получают порт памяти, не могут внедрять системные правила
        и не могут напрямую записывать долговременную память.
      </p>

      <h2>Службы хоста</h2>
      <div className="overflow-x-auto">
        <table>
          <thead><tr><th>Служба</th><th>Контракт</th><th>Разрешение</th></tr></thead>
          <tbody>
            <tr><td><code>storage</code></td><td>KV в пространстве имён, коллекции документов и уведомления о внешних изменениях.</td><td>Нет</td></tr>
            <tr><td><code>secrets</code></td><td>Зашифрованные слоты учётных данных в пространстве имён.</td><td>Нет</td></tr>
            <tr><td><code>ui</code></td><td>Уведомление хоста и поток сохранения/экспорта.</td><td>Нет</td></tr>
            <tr><td><code>schedules</code></td><td>Привязка обработчика к объявленной в манифесте периодичности.</td><td>Нет</td></tr>
            <tr><td><code>session</code></td><td>Подписка на ограниченные сведения о сессии чтения.</td><td>Нет</td></tr>
            <tr><td><code>network</code></td><td>HTTP через хост.</td><td><code>service:network</code></td></tr>
            <tr><td><code>llm</code></td><td>Одноразовые вызовы модели с текстовым результатом или ограничением по JSON-схеме, использующие конфигурацию пользователя.</td><td><code>service:llm</code></td></tr>
            <tr><td><code>clipboard</code></td><td>Запись текста в системный буфер обмена.</td><td><code>service:clipboard</code></td></tr>
          </tbody>
        </table>
      </div>

      <h3>Хранилище</h3>
      <p>
        Используйте KV для небольших настроек и контрольных точек. Используйте именованную
        коллекцию документов для записей плагина со стабильными ID и необязательным происхождением
        <code>bookId</code>/<code>anchor</code>. Происхождение — это индекс, а не владение;
        документ может пережить удаление указанной книги. При удалении плагина коллекции документов
        очищаются, но KV, слоты секретов и зафиксированные метаданные схемы сохраняются для
        повторной установки и миграции.
      </p>

      <h3>Расписания</h3>
      <p>
        Манифест объявляет <code>{`{ id, label, everyMinutes }`}</code>, а активация
        привязывает обработчик через <code>ctx.services.schedules.bind</code>.
        Минимальная периодичность — 15 минут. Пока приложение открыто, запуски происходят
        не реже этой периодичности, наверстывают отставание после запуска и не пересекаются.
        Это не постоянная фоновая задача и не гарантия выполнения в точное время.
      </p>

      <h2>Декларативный интерфейс и настройки</h2>
      <p>
        Плагины возвращают версионированные данные представления, а не исполняемый UI.
        Грамматика представлений включает markdown, списки с поиском, формы, подробные
        макеты, результаты словаря и ограниченные деревья блоков. Обработчики могут
        сохранить поверхность, показать уведомление, открыть или заменить представление,
        сбросить навигацию, закрыть поверхность или вернуть ошибки полей. Хост отвечает
        за состояния загрузки и ошибок промисов.
      </p>
      <p>
        Настройки манифеста используют элементы управления хоста для текстовых полей,
        многострочного текста, чисел, времени, списков, вариантов, флажков, переключателей
        и секретных полей. Условные поля используют <code>visibleWhen</code>, а динамические
        списки — зарегистрированный провайдер <code>settingsOptions</code>. Секретные поля
        записываются непосредственно в зашифрованные слоты секретов и никогда не попадают
        в обычный объект настроек или каталог, видимый агенту.
      </p>

      <h2>Темы и шрифты</h2>
      <p>
        Плагины тем объявляют смысловые данные в манифесте. Тема приложения переопределяет
        фиксированный словарь токенов хоста; тема ридера предоставляет обязательную
        шестицветную палитру страницы и необязательные типографические значения по умолчанию.
        Хост проверяет значения, генерирует CSS, загружает одобренные локальные файлы шрифтов
        и ничего не применяет, пока пользователь не выберет тему.
      </p>
      <p>
        Для предоставления вариантов требуется <code>ui:themes</code>. Для выбора одного
        из них требуется точное предоставление записи настроек, например{" "}
        <code>appearance.theme</code> или <code>reading.theme</code>. Одно не подразумевает другое.
      </p>

      <h2>Фазы жизненного цикла</h2>
      <ol>
        <li><strong>Активация:</strong> доступны запросы и приватное чтение плагина; регистрации подготовлены; побочные эффекты заблокированы.</li>
        <li><strong>Миграция:</strong> доступны только KV и коллекции документов плагина.</li>
        <li><strong>Активен:</strong> продвинутые обработчики могут использовать предоставленные им домены, расширения и службы.</li>
      </ol>
      <p>
        Хост обрабатывает RPC активации, проверяет Worker, выполняет миграцию данных,
        а затем в одной явной точке продвигает весь подготовленный набор. Ошибка активации
        освобождает подготовленную работу, не заменяя текущую среду выполнения.
      </p>

      <h2>Окружение Worker</h2>
      <p>
        Доступа к React, Jotai, DOM, WebView, Tauri, SQLite, файловой системе и
        процессам нет. Неявные <code>fetch</code>, WebSocket, EventSource,
        XMLHttpRequest, BroadcastChannel, IndexedDB и Cache Storage отключены.
        Используйте типизированный контекст для сети, сохранения и каждого взаимодействия
        с хостом.
      </p>

      <h2>Совместимость и стабильность</h2>
      <p>
        Домены, расширения, службы и декларативные схемы имеют независимые семантические
        версии. Неизвестные ID, недопустимые диапазоны semver, недоступные требуемые
        возможности и несовместимые версии хоста препятствуют активации. Совместимые
        дополнения повышают версию соответствующей возможности, а не единый глобальный
        номер API плагинов.
      </p>
      <p>
        Текущая экосистема состоит из официальных плагинов, поэтому действующий
        контракт, поддерживаемый реестром, является исходной точкой. Не опирайтесь
        на прежние формы <code>shelf</code>, <code>appearance</code> или формы,
        существовавшие до появления реестра.
      </p>
    </article>
  );
}
