import { Link, createFileRoute } from "@tanstack/react-router";
import { MARKETPLACE_REPO_URL } from "../../../../lib/site";

export const Route = createFileRoute("/ru/docs/plugins/develop")({
  head: () => ({
    meta: [
      { title: "Создание плагина — Документация ReadAware" },
      {
        name: "description",
        content:
          "Создавайте, проверяйте, устанавливайте, мигрируйте и тестируйте плагин ReadAware с помощью публичного шаблона TypeScript.",
      },
    ],
  }),
  component: DevelopPluginPage,
});

function DevelopPluginPage() {
  return (
    <article className="doc-prose">
      <h1>Создание плагина</h1>
      <p className="lead">
        Начните с публичного шаблона TypeScript, объявите минимальный набор
        возможностей и протестируйте собранный пакет в настольном приложении ReadAware.
        Хост отвечает за жизненный цикл, разрешения, представление и откат;
        плагин отвечает за собственное поведение и приватные данные.
      </p>

      <h2>Предварительные требования</h2>
      <ul>
        <li>Настольное приложение ReadAware с доступом к разделу «Настройки → Плагины».</li>
        <li><a href="https://bun.sh" target="_blank" rel="noopener noreferrer">Bun</a> для скриптов репозитория.</li>
        <li>Локальная копия или fork репозитория{" "}
          <a href={MARKETPLACE_REPO_URL} target="_blank" rel="noopener noreferrer">readaware-plugins</a>.
        </li>
      </ul>

      <h2>Создайте пакет</h2>
      <ol>
        <li>Скопируйте <code>template/</code> в <code>plugins/&lt;your-plugin-id&gt;/</code>.</li>
        <li>Сделайте имя папки, идентификатор <code>id</code> манифеста и пространство имён среды выполнения одинаковыми.</li>
        <li>Измените <code>manifest.json</code> и <code>src/main.ts</code>.</li>
        <li>Удалите неиспользуемые расширения шаблона и соответствующие им разрешения.</li>
        <li>Соберите автономный <code>main.js</code>, который загружает ReadAware.</li>
      </ol>
      <pre><code>{`bun run build
bun run typecheck
bun test
bun run validate`}</code></pre>

      <h2>Спроектируйте манифест до реализации</h2>
      <p>Проверяйте манифест в следующем порядке:</p>
      <ol>
        <li><strong>Идентичность</strong> — стабильный ID, имя, версия пакета, автор и минимальная версия приложения.</li>
        <li><strong>Данные</strong> — положительное целое число <code>schemaVersion</code> и путь миграции.</li>
        <li><strong>Совместимость</strong> — диапазон semver в <code>requires</code> для каждого используемого API и схемы.</li>
        <li><strong>Полномочия</strong> — семантические <code>permissions</code> и точные предоставления <code>settingsAccess</code>.</li>
        <li><strong>Декларации</strong> — настройки, расписания, темы, шрифты и модуль входа.</li>
      </ol>
      <p>
        Перед установкой используйте{" "}
        <Link to="/ru/docs/plugins/capabilities">каталог возможностей и предварительный просмотр разрешений</Link>.
        Требования заявляют о совместимости, а не предоставляют полномочия пользователя;
        возможности без разрешений всё равно должны быть указаны в <code>requires</code>,
        если плагин зависит от их контракта.
      </p>

      <h2>Выберите подходящую возможность</h2>
      <ol>
        <li>Используйте <strong>домен</strong> для состояния или поведения, которым владеет ReadAware.</li>
        <li>Используйте <strong>расширение</strong>, чтобы предоставить вариант, действие или провайдера.</li>
        <li>Используйте <strong>службу</strong> для ограниченной операции хоста.</li>
        <li>Используйте хранилище плагина только для данных, принадлежащих плагину.</li>
        <li>Запросите новую типизированную возможность хоста, если ни одна существующая форма не подходит.</li>
      </ol>
      <p>
        Не дублируйте книги, прогресс, аннотации, настройки или память в хранилище
        плагина. Теневое состояние обходит инварианты продукта, зафиксированные события,
        восстановление проекций, семантику синхронизации и контекст агента.
      </p>

      <h2>Сохраняйте активацию декларативной</h2>
      <p>
        Во время <code>activate(ctx)</code> изучите окружение и зарегистрируйте
        действия, команды, провайдеров, подписки и расписания. Не выполняйте
        бизнес-записи или внешнюю работу. Хост поэтапно подготавливает каждую
        регистрацию, пока не завершатся RPC активации и Worker не ответит на
        проверочный ping.
      </p>
      <p>
        Запускайте работу среды выполнения из зарегистрированного обработчика после
        продвижения. Если обработчик возвращает promise, позвольте хосту показывать
        состояния загрузки и ошибки. Храните ссылки на внешние ресурсы только если
        ваш необязательный <code>deactivate()</code> должен их закрыть; регистрации
        и подписки хоста освобождаются автоматически.
      </p>

      <h2>Явно версионируйте приватные данные</h2>
      <p>
        <code>schemaVersion</code> версионирует KV и коллекции документов плагина;
        эта версия не зависит от версии пакета. Изменяйте её только при изменении
        структуры приватных данных. Экспортируйте <code>migrate(storageCtx, change)</code>{" "}
        для каждого поддерживаемого обновления и отката после фиксации схемы.
      </p>
      <ul>
        <li>Миграции получают только хранилище: без доменов, настроек, секретов, сети, UI, LLM и расширений.</li>
        <li>Делайте каждый переход детерминированным и идемпотентным.</li>
        <li>Проверьте сбой после частичных записей: хост должен точно восстановить KV, документы, файлы и метаданные схемы.</li>
        <li>Не используйте проверку версии пакета вместо схемы данных.</li>
      </ul>

      <h2>Установите рабочую папку</h2>
      <ol>
        <li>Запустите сборку и проверки.</li>
        <li>Откройте ReadAware → Настройки → Плагины → Установить плагин.</li>
        <li>Выберите папку собранного плагина и изучите сводку согласия.</li>
        <li>Проверьте реальную функцию в настольном приложении.</li>
        <li>Пересоберите и переустановите плагин, чтобы проверить обновление.</li>
      </ol>
      <p>
        Обычный браузер не может проверить установку плагина, IPC Worker, сохранение
        в SQLite, доступ к исходным книгам, интеграцию с ридером или откат. Проверяйте
        поставляемое приложение Tauri.
      </p>

      <h2>Тестируйте жизненный цикл, а не только успешный сценарий</h2>
      <ul>
        <li>Чистая установка, включение, отключение и повторное включение без перезапуска.</li>
        <li>Успешное обновление и откат на реальных данных.</li>
        <li>Тайм-аут активации, отклонение обработчика, ошибка миграции и точный откат.</li>
        <li>Очистка при удалении: не должно остаться действий, слушателей, расписаний, провайдеров или Worker.</li>
        <li>Удаление и расширение разрешений во время обновления.</li>
        <li>Длинные метки, пустые состояния, навигация с клавиатуры и все темы хоста.</li>
      </ul>

      <h2>Знайте текущие ограничения</h2>
      <p>
        Расписания выполняются, пока ReadAware открыт, как минимум с объявленной
        периодичностью, а при отставании наверстываются после запуска. Это не постоянные
        фоновые задачи: при закрытом приложении выполнение не происходит, нет постоянной
        очереди, контракта повторов/отката или гарантии возобновления после сбоя.
      </p>
      <p>
        UI доступен только в существующих типизированных точках расширения. Для
        отсутствующего места нужны расширение и потребитель, принадлежащие хосту;
        произвольный HTML или универсальный нативный API invoke не будут добавлены
        в качестве обходного пути.
      </p>

      <h2>Далее</h2>
      <p>
        Держите <Link to="/ru/docs/plugins/api">справочник API</Link> рядом с редактором,
        затем перед подготовкой pull request для реестра ознакомьтесь с разделом{" "}
        <Link to="/ru/docs/plugins/publishing">«Публикация»</Link>.
      </p>
    </article>
  );
}
