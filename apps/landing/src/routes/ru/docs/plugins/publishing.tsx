import { Link, createFileRoute } from "@tanstack/react-router";
import { MARKETPLACE_REPO_URL } from "../../../../lib/site";

export const Route = createFileRoute("/ru/docs/plugins/publishing")({
  head: () => ({
    meta: [
      { title: "Публикация плагина — Документация ReadAware" },
      {
        name: "description",
        content:
          "Как отправить плагин в каталог ReadAware: структура репозитория, валидация и требования ревью.",
      },
    ],
  }),
  component: PublishingPage,
});

function PublishingPage() {
  return (
    <article className="doc-prose">
      <h1>Публикация плагина</h1>
      <p className="lead">
        Каталог работает как репозиторий расширений Raycast: ваш плагин живёт в
        публичном{" "}
        <a href={MARKETPLACE_REPO_URL} target="_blank" rel="noopener noreferrer">
          readaware-plugins
        </a>{" "}
        и попадает туда через пулл-реквест. После слияния он появляется в
        приложении в разделе Настройки → Плагины → Каталог и устанавливается
        одним кликом.
      </p>

      <h2>Разработка на TypeScript</h2>
      <p>
        TypeScript — рекомендуемый путь. Репозиторий содержит <code>template/</code>{" "}
        с уже подключённым типизированным API (<code>types/plugin-api.d.ts</code>) —
        скопируйте его, напишите <code>src/main.ts</code> и соберите единый
        самодостаточный модуль:
      </p>
      <pre>
        <code>bun build src/main.ts --outfile main.js --format esm</code>
      </pre>
      <p>
        В поставку всегда идёт собранный <code>main.js</code>; храните{" "}
        <code>src/</code> в репозитории, чтобы ревьюеры могли прочитать настоящий
        код. Обычный JavaScript также принимается. Официальные плагины в{" "}
        <code>plugins/</code> написаны именно так — используйте их как живые
        примеры.
      </p>

      <h2>Отправка</h2>
      <ol>
        <li>Сделайте форк репозитория.</li>
        <li>
          Скопируйте <code>template/</code> в{" "}
          <code>plugins/&lt;your-plugin-id&gt;/</code>, минимум с{" "}
          <code>manifest.json</code> и <code>main.js</code>. Имя папки должно
          совпадать с <code>id</code> манифеста.
        </li>
        <li>
          Добавьте соответствующую запись в <code>registry.json</code>, сохраняя
          массив отсортированным по id.
        </li>
        <li>
          Запустите те же проверки, что будет запускать CI:
          <pre>
            <code>{`node scripts/validate.mjs
npx tsc --noEmit`}</code>
          </pre>
        </li>
        <li>
          Откройте пулл-реквест с описанием того, что делает плагин и зачем ему
          каждое объявленное разрешение.
        </li>
      </ol>
      <p>
        CI проверяет согласованность реестра и манифеста, формат id, белый список
        разрешений и наличие файлов, а также прогоняет проверку типов для каждого
        TypeScript-плагина.
      </p>

      <h2>Обновления</h2>
      <p>
        Тот же процесс: поднимите <code>version</code> в{" "}
        <code>manifest.json</code> и <code>registry.json</code> одним
        пулл-реквестом. Учтите, что приложение читает реестр через CDN, поэтому
        слитое обновление может появиться во вкладке каталога с небольшой
        задержкой.
      </p>

      <h2>Требования ревью</h2>
      <ul>
        <li>
          Объявляйте минимальные разрешения. Пулл-реквесты, запрашивающие больше,
          чем использует код, будут возвращены — см.{" "}
          <Link to="/ru/docs/plugins/api">таблицу разрешений</Link>.
        </li>
        <li>
          <code>main.js</code> должен быть читаемым или сопровождаться
          исходниками, из которых он собран.
        </li>
        <li>
          Никакого обфусцированного кода, аналитики и трекинга, никакой загрузки
          кода с удалённых серверов.
        </li>
      </ul>
      <p>
        Плагины работают внутри приложения с тем же доступом, что и само
        приложение. Установка — это решение о доверии, которое пользователь
        принимает отдельно для каждого плагина, и это ревью — первая линия обороны
        сообщества — пишите плагины, которые вы сами не побоялись бы установить у
        незнакомца.
      </p>
    </article>
  );
}
