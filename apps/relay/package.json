{
  "name": "@read-aware/relay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var MAGIC_LINK_ECHO:1",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@read-aware/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "wrangler": "^4.33.1"
  }
}
