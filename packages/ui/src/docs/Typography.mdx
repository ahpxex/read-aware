import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Design System/Guidelines/Typography" />

# Typography

RadAware uses two typefaces: **Inter** (sans-serif) for UI and body text, and the
system **serif** stack for display headings.

## Type scale

| Component | Font | Size | Weight | Tracking | Leading | Color |
|-----------|------|------|--------|----------|---------|-------|
| Display | Serif | 5xl / 6xl / 7xl | Normal | Tight | 0.98 | stone-950 |
| Heading | Sans | xl / 2xl / 3xl / 4xl | Semibold | Tight | Default | stone-950 |
| Body | Sans | base / lg | Normal | Default | 2rem | stone-700 |
| Eyebrow | Sans | 13px | Medium | Default | Default | stone-500 |
| Caption | Sans | 12px | Normal | Default | Default | stone-500 |

## Design tokens

```
--font-size-eyebrow: 0.6875rem   (11px)
--font-size-caption: 0.75rem     (12px)
--leading-display: 0.98
--leading-body: 2rem
```

## Guidelines

- **Display** is reserved for hero-level headings. One per view maximum.
- **Eyebrow** labels are small, quiet section and metadata labels (sentence case, no wide tracking).
- **Body** text uses generous line height (2rem) for comfortable reading.
- Avoid mixing serif and sans within the same text block.
- Never use bold for emphasis in body text -- use structure (headings, eyebrows)
  to create hierarchy instead.
